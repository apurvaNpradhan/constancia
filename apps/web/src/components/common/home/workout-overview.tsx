function WorkoutOverview() {
   return (
      <div>
         Will contain all the workouts made + option to add a new workout which will create a new
         workout for the date picked or if create for today is select it will create for today and
         open /workout/$id
      </div>
   );
}

export default WorkoutOverview;
