import { TestingNote } from "@/components/testing-note";

function JournalOverview() {
   return (
      <div>
         Will contain all the journal entries made + option to add a new entry which will createa
         new entry for the date picked or if create for today is select it will create for today and
         open /journal/$id
      </div>
   );
}

export default JournalOverview;
