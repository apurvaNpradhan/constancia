function HabitOverview() {
   return (
      <div>
         Will contain all the habits made + option to add a new habit which will create a new habit
         for the date picked or if create for today is select it will create for today and open
         /habit/$id
      </div>
   );
}

export default HabitOverview;
